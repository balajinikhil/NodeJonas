exports.getUsers = (req, res) => {
  res.status(500).json({
    status: "route still not active"
  });
};

exports.createUsers = (req, res) => {
  res.status(500).json({
    status: "route still not active"
  });
};

exports.getUser = (req, res) => {
  res.status(500).json({
    status: "route still not active"
  });
};

exports.editUser = (req, res) => {
  res.status(500).json({
    status: "route still not active"
  });
};

exports.deleteUser = (req, res) => {
  res.status(500).json({
    status: "route still not active"
  });
};
